# Streams Admin UI (Static Demo)

Light-theme admin portal UI inspired by Streams.AI home dashboard.

## Run
```bash
npm install
npm run dev
```

Then open the URL shown in the terminal (usually http://localhost:5173).

## Notes
- UI is static (clicks show alerts).
- Tailwind is configured.
- Framer Motion is used for subtle transitions.
